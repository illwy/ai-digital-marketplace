# 采用模块化单体，不引入微服务

规划文档要求简单架构、单体优先。数字商品自动售卖的核心路径是「下单 → 支付回调 → 锁库存 → 交付」，这些步骤必须同库事务或明确补偿，拆成多个服务只会把一致性问题变成分布式事务。

考虑过把支付和交付拆成独立服务，以及用消息队列串联。4 核 8G 单机、MVP 阶段没有吞吐证据，消息队列和 Kubernetes 被明确排除。模块按包边界拆分，以后要拆服务时按包迁出即可。

**Status:** accepted
