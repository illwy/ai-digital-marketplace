# 用户端与管理端拆成两个 Vue 3 应用

用户商城要像商店，管理后台要像运营台。合成一个应用会把路由守卫、布局、组件库皮肤和打包体积缠在一起。两个 Vite 应用共享 TypeScript 类型约定，都走同一套 `/api/v1` 后端。

考虑过单应用按角色切换布局，以及用户端改用 Nuxt SSR。SEO 对卡密类商品不是 MVP 约束；单应用更省脚手架，但后台与前台视觉目标冲突。Element Plus 主要用于管理端；用户端可用同一组件库但独立主题，避免整站长成后台。

**Status:** accepted
