spring:
  datasource:
    url: jdbc:mysql://localhost:3307/marketplace?useUnicode=true&characterEncoding=utf8&serverTimezone=Asia/Shanghai&allowPublicKeyRetrieval=true&useSSL=false
  data:
    redis:
      host: localhost

app:
  jwt:
    secret: ${JWT_SECRET:dev-local-jwt-secret-key-not-for-prod}
  admin:
    password: ${ADMIN_PASSWORD:dev-local-admin-password}
